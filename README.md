My personal website built with [Gatsby](https://www.gatsbyjs.org) and its default starter kit.

---
Notes to self:

Using styled components with SASS variables.
`${props => props.theme.variableName}`
